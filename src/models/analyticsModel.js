export default class analyticsModel {
    constructor(threshold = '', documents = [], report = []) {
        this.threshold = threshold;
        this.documents = documents;
        this.report = report;
    }
}